import { Routes } from '@angular/router';
import { ObservableOfFromComponent } from './observable-of-from/observable-of-from.component';

export const routes: Routes = [
    {
        path:'rxjs-of-from',
        component:ObservableOfFromComponent
    }
];
